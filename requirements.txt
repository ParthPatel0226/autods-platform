# =============================================================================
# AutoDS Platform — Streamlit Cloud Dependencies (slim)
# For full local dev dependencies, see requirements-full.txt
# =============================================================================

# --- Dashboard ---
streamlit>=1.40.0
pandas>=2.2.0
numpy>=1.26.0
plotly>=5.24.0

# --- Auth & Database ---
supabase>=2.0.0

# --- Utilities (needed by core modules at import time) ---
pyyaml>=6.0.0
python-dotenv>=1.0.0
